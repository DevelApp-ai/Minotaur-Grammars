.alert {
  color: #c00;
  .bold-text();
}

# --- Coverage completion constructs ---

.mixin() { box-shadow+: 0 0 2px black; }
.card { box-shadow+: 1px 1px 4px gray; }
@prop: color; .d { @{prop}: red; }
.btn {&:hover { color: red; } & + & { margin-left: 8px; } }
@media print { .hide { .nested { display: none; } } }
