void main() {
  final items = [1, 2, 3];
  print(items.where((x) => x > 1).length);
}

# --- Coverage completion constructs ---

extension StringX on String { String get shout => toUpperCase() + '!'; }
(int, String) pair = (1, 'one');
final (a, b) = (1, 2);
switch (obj) { case int x when x > 0: print(x); case [var first, ...]: print(first); default: }
sealed class Shape {}
Stream<int> tick() async* { yield 1; yield 2; }
late final result = compute();
part 'src/util.dart';
part of 'library.dart';
