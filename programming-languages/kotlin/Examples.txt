fun max(a: Int, b: Int): Int = if (a > b) a else b

# --- Extended constructs ---
interface Repo { fun find(id: Int): User? }
sealed class Result
data class Ok(val v: Int) : Result()
companion object { val DEFAULT = Ok(0) }
lateinit var cache: String
suspend fun load(): User = fetch()
when { x > 0 -> "pos" else -> "neg" }
@Deprecated("old") fun old() {}

