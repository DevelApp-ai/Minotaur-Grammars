fun max(a: Int, b: Int): Int = if (a > b) a else b

# --- Coverage completion constructs ---

inline fun <reified T> tag(): String = T::class.simpleName ?: "?"
crossinline body check
fun sum(vararg values: Int) = values.sum()
operator fun Times.dec() = this - 1
infix fun Int.pow(n: Int): Int = if (n == 0) 1 else this * pow(n - 1)
tailrec fun fac(n: Long, acc: Long = 1): Long = if (n <= 1) acc else fac(n - 1, acc * n)
data object Empty : List<Nothing>
list.map { it * 2 }
