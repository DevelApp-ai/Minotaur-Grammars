my @nums = (1, 2, 3);
foreach my $n (@nums) {
  print "$n\n";
}

# --- Coverage completion constructs ---

my $grade = $score >= 50 ? 'pass' : 'fail';
print "$_\n" for @lines;
next unless defined $x;
my @first3 = @items[0..2];
my @vals = %map{'a','b'};
if ($text =~ m/hello\s+world/) { ... }
sub add ($x, $y = 1) { return $x + $y }
