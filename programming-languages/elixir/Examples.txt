"hello"
|> String.upcase()
|> String.split()

defmodule Foo do
  def bar(x) when is_integer(x), do: x + 1
end

# --- Extended constructs ---
defmodule Demo do
  @behaviour Handler
  alias Foo.Bar
  use GenServer
  @default 10
  def run(%{name: name}) when is_binary(name), do: name
  def run(_), do: @default
end
with {:ok, v} <- fetch() do v else e -> e end
for x <- 1..3, into: [], do: x * x

