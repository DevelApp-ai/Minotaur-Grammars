local t = { x = 1, y = 2 }
for k, v in pairs(t) do
  print(k, v)
end

# --- Coverage completion constructs ---

local a, b, c = 1, 2
local x, y = f()
return 1, 2, 3
local n = select('#', ...)
goto skip ::skip:: print('here')
local s = "line\nbreak\ttab\"quote\""
--[==[ long comment ]==]
