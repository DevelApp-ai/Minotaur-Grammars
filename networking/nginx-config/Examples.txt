server {
  listen 443 ssl;
  server_name example.com;
  location / { root /var/www/html; }
}

# --- Coverage completion constructs ---
geo $country { default ZZ; 10.0.0.0/8 EU; }
split_clients "${remote_addr}" $variant { 10% .one; * .two; }
types { application/json json; }
upstream app { least_conn; server a:8080 weight=3 max_fails=2 fail_timeout=10s; }

