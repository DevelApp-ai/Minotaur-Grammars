global
  log stdout format raw local0
frontend https
  bind *:443 ssl crt /certs/site.pem
  default_backend servers

# --- Coverage completion constructs ---
resolvers dns
  nameserver d1 10.0.0.1:53
frontend f
  bind *:443
  http-response set-header X-Frame-Options DENY
  redirect scheme https if !{ ssl_fc }
  tcp-request connection reject if { src -f /etc/blacklist }
backend b
  server s1 a:80 cookie s1 check inter 2s observe layer7

