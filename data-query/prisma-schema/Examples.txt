datasource db { provider = "postgresql" url = env("DATABASE_URL") }

generator client { provider = "prisma-client-js" }

# --- Extended constructs ---
model Post {
  id Int @id
  authorId Int
  author User @relation(fields: [authorId], references: [id], onDelete: Cascade)
  @@index([authorId])
  @@unique([slug])
  @@map("posts")
}
view UserView { id Int name String }

# --- Coverage completion constructs ---

generator client { provider = "prisma-client-js" previewFeatures = ["views"] multiSchema = true }
model Event { id Int @id @db.BigInt ts DateTime @db.Timestamptz(6) name String @db.VarChar(255) }
