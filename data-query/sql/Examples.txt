CREATE TABLE users (id INTEGER PRIMARY KEY, name TEXT NOT NULL);
INSERT INTO users (id, name) VALUES (1, 'Ada');
SELECT * FROM users WHERE id = 1;

# --- Coverage completion constructs ---

INSERT INTO users (id, name) VALUES (1, 'a') ON CONFLICT (id) DO UPDATE SET name = EXCLUDED.name;
MERGE INTO t USING s ON t.id = s.id WHEN MATCHED THEN UPDATE SET v = s.v WHEN NOT MATCHED THEN INSERT VALUES (s.id, s.v);
WITH del AS (DELETE FROM log WHERE ts < '2026-01-01' RETURNING *) SELECT count(*) FROM del;
CREATE TRIGGER trg BEFORE INSERT ON t FOR EACH ROW EXECUTE FUNCTION fn();
CREATE PROCEDURE p(IN x INT) BEGIN SELECT x; END;
GRANT SELECT, INSERT ON t TO analyst;
SELECT data->>'name', data#>'{a,b}' FROM docs;
SELECT * FROM a, LATERAL (SELECT * FROM b WHERE b.id = a.id) sub;
SELECT avg(v) OVER w FROM t WINDOW w AS (PARTITION BY g ORDER BY ts ROWS BETWEEN 1 PRECEDING AND CURRENT ROW);
