404: Not Found

# --- Extended constructs ---
class apache ( $port = 80 ) {
  package { 'httpd': ensure => installed }
  @service { 'httpd': ensure => running }
}
service { 'httpd': require => Package['httpd'] }
case $os { debian: { include apt } }

