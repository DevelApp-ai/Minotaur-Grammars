404: Not Found

# --- Extended constructs ---
template '/etc/app.conf' do
  source 'app.conf.erb'
  variables(key: node['app']['key'])
  notifies :restart, 'service[app]', :immediately
  not_if { ::File.exist?('/etc/app.conf') }
end

