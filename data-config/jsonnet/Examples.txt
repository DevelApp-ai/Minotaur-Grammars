local env = "prod";
{
  name: "order-service",
  replicas: if env == "prod" then 3 else 1,
  labels+: { tier: "backend" },
  ports: [8080, 9090],
  env: [ { name: k, value: v } for k, v in { log: "debug", region: "eu" } ],
  config: import "config.jsonnet",
  getPort(i):: self.ports[i],
}

# --- Extended constructs ---
local base = { a: 1 };
base + { b: self.a + 1 }
[ x * 2 for x in [1, 2, 3] if x > 1 ]
assert 1 + 1 == 2 : "math is broken";
function(a, b=2) a + b

