local env = "prod";
{
  name: "order-service",
  replicas: if env == "prod" then 3 else 1,
  labels+: { tier: "backend" },
  ports: [8080, 9090],
  env: [ { name: k, value: v } for k, v in { log: "debug", region: "eu" } ],
  config: import "config.jsonnet",
  getPort(i):: self.ports[i],
}

# --- Coverage completion constructs ---
{ base + override: super.a, assert self.n > 0 }
std.format("%s!", "hi")
local x = 1; x + 1
arr[1:3]
-1

