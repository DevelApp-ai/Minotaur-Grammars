name = "my-service"
server {
  port = 8080
  host = ${env.HOST}
  timeout = 30s
}
database.url = "postgres://db/example"
replicas = [1, 2, 3]
include "base.conf"

# --- Coverage completion constructs ---
list = [1, 2] [3]
items += "four"
"quoted.key" = 1

